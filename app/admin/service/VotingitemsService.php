<?php 


namespace app\admin\service;


use app\admin\model\Votingitems;

/**
 * 投票选项管理-服务类
 * @author PENGKING
 * @since: 2022/07/25
 * Class VotingitemsService
 * @package app\admin\service
 */
class VotingitemsService extends BaseService
{
    /**
     * 构造函数
     * LevelService constructor.
     */
    public function __construct()
    {
        $this->model = new Votingitems();
    }
	
	/**
     * 获取数据列表
     * @return array
     * @since 2022/07/25
     * @author PENGKING
     */
    public function getList()
    {
        $param = request()->param();

        // 查询条件
        $map = [];
		
	
	    // 标题
        $title = isset($param['title']) ? trim($param['title']) : '';
        if ($title) {
            $map[] = ['title', 'like', "%{$title}%"];
        }
	
        return parent::getList($map); // TODO: Change the autogenerated stub
    }

	/**
     * 添加或编辑
     * @return array
     * @since 2022/07/25
     * @author PENGKING
     */
    public function edit()
    {
        // 参数
        $data = request()->param();
	                                                                                                                                                        
		// 图片地址处理
        $images = getter($data, 'images');
        if (!empty($images)) {
            if (strpos($images, "temp") !== false) {
                $data['images'] = save_image($images, 'votingitems');
            } else {
                $data['images'] = str_replace(IMG_URL, "", $images);
            }
        }
                                                                                    
        return parent::edit($data); // TODO: Change the autogenerated stub
    }

                                                                            
        /**
        * 获取数据列表
        * @return array
        * @since 2022/07/25        * @author PENGKING        */
        public function getListSelect()
        {
            return parent::getListSelect();
        }

}